# Insert blank line betweeen shell prompts
add_newline = true

# Replace the "❯" symbol in the prompt with "➜"
[character]
success_symbol = "[→ ](green)"
error_symbol = "[→ ](red)"

[directory]
truncation_length = 8
